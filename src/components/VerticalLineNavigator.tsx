import * as React from 'react';
import {Box} from '@mui/material';

export interface IVerticalLineNavigatorProps {
}

const VerticalLineNavigator = (props: IVerticalLineNavigatorProps) => {

  return (
    <Box>

    </Box>
  );
};

export default VerticalLineNavigator;
